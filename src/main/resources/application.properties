spring.application.name=JobSearch

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/jobsearch?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=030203

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf Template Settings
spring.thymeleaf.cache=false

# File Upload Settings (Vulnerable - no file type checking)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB